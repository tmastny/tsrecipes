---
title: "discrete-cosine-transform"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{discrete-cosine-transform}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  message = FALSE,
  warning = FALSE
)
```

```{r, child="discrete-cosine-transform_child.Rmd"}
```

  # TODO:
  #   Add the following examples:
        1. where you want to return a different `k` coefficients, so you  
           apply `step_dct` twice
           
        2. You want to use a different selection procedure, so you return 
           `k = length(ts)`.
  
